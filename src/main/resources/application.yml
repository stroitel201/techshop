spring:
  jpa:
    show-sql: true
    generate-ddl: false
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL57Dialect

---
spring:
  profiles: default
  datasource:
    url: jdbc:mysql://localhost:3306/springshop?serverTimezone=UTC
    driverClassName: com.mysql.jdbc.Driver
    username: root
    password: 1234